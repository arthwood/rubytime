%tr{:class => row_class(project_counter), :title => project.description}
  %td= project.name
  %td= project.client.name
  %td.calign= verbalize(project.active)
  %td.actions.ralign
    = link_to image_tag('pencil.png'), edit_project_url(project)
    = link_to image_tag('cross.png'), project_url(project)
