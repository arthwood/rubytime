.activities
  = render :partial => 'cell_activity', :collection => activities, :as => :activity
%p.total
  Total:
  %span
    = Rubytime::Util.format_time_spent(activities.inject(0) {|mem, i| mem + i.minutes})
