%tr{:class => row_class(client_user_row_counter)}
  %td= user.name
  %td= user.client.name
  %td.centered= verbalize(user.active)
  %td.actions
    = link_to image_tag('pencil.png'), edit_user_url(user)
    = link_to image_tag('cross.png'), user_url(user)
