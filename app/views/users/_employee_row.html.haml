%tr{:class => row_class(user_counter)}
  %td= user.name
  %td= user.role.name
  %td.calign= verbalize(user.active)
  %td.actions.ralign
    = link_to image_tag('pencil.png'), edit_user_url(user)
    = link_to image_tag('cross.png'), user_url(user)
